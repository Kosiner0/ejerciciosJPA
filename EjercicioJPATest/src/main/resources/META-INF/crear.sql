CREATE TABLE noticia (titulo VARCHAR(255) PRIMARY KEY,autor VARCHAR(255) NOT NULL,fecha DATE NOT NULL);
CREATE TABLE comentario (id BIGINT AUTO_INCREMENT PRIMARY KEY,texto TEXT NOT NULL,autor VARCHAR(255) NOT NULL,noticia_titulo VARCHAR(255),FOREIGN KEY (noticia_titulo) REFERENCES noticia(titulo));